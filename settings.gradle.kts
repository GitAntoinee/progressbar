rootProject.name = "progressbar"
pluginManagement {
    repositories {
        maven("https://dl.bintray.com/kotlin/kotlin-eap")
        gradlePluginPortal()
    }
}

enableFeaturePreview("GRADLE_METADATA")
